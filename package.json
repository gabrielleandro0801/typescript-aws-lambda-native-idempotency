{
  "name": "typescript-aws-lambda-native-idempotency",
  "version": "1.0.0",
  "description": "Repository with an example of AWS Lambda using its native idempotency validation in Typescript",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc --build && npm prune --production",
    "fn-wrapper-sqs-consumer-full-payload": "ts-node-dev --respawn --transpile-only --ignore-watch node_modules --no-notify src/localTests/fn-wrapper/sqs-full-payload-idempotent.ts",
    "fn-wrapper-sqs-consumer-message-id": "ts-node-dev --respawn --transpile-only --ignore-watch node_modules --no-notify src/localTests/fn-wrapper/sqs-message-id-idempotent.ts",
    "fn-wrapper-sqs-consumer-body": "ts-node-dev --respawn --transpile-only --ignore-watch node_modules --no-notify src/localTests/fn-wrapper/sqs-body-idempotent.ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gabrielleandro0801/typescript-aws-lambda-native-idempotency.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/gabrielleandro0801/typescript-aws-lambda-native-idempotency/issues"
  },
  "homepage": "https://github.com/gabrielleandro0801/typescript-aws-lambda-native-idempotency#readme",
  "devDependencies": {
    "@types/node": "^18.13.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "@aws-lambda-powertools/idempotency": "^1.17.0",
    "@aws-sdk/client-dynamodb": "^3.477.0",
    "@aws-sdk/client-sqs": "^3.477.0",
    "@aws-sdk/lib-dynamodb": "^3.477.0",
    "@smithy/node-http-handler": "^2.2.1",
    "@types/aws-lambda": "^8.10.130",
    "install": "^0.13.0",
    "npm": "^10.2.5"
  }
}